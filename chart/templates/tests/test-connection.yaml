apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appofapps.fullname" . }}-test-connection"
  labels:
    {{- include "appofapps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appofapps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
